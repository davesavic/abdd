tests:
  - name: Create new business
    description: Test the creation of a new business
    depends:
      - Register account
    fake:
      name: "{businessname}"
    request:
      headers:
        Authorization: Bearer ${access_token}
      method: POST
      url: /businesses
      body: |
        {
          "name": "${name}"
        }
    expect:
      status: 201
    extract:
      - path: id
        as: business_id
